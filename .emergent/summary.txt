<analysis>**original_problem_statement**: 
Build a software solution for an NBFC-MFI app named Bahi Khata transitioning to a Sahukar Illaka model. Ensure the Collection Sheet (Vasuli) strictly operates on a Financial Year (FY) view, handling complex Net-off loans gracefully.
PRODUCT REQUIREMENTS:
- Roles Hierarchy: Admin -> Maalik -> Muneem -> Sipahi.
- Accounts Module: Full double-entry accounting.
- Collection Sheet: FY view, combines Net-off loans, calculates retroactive balances.
- Comprehensive Maalik-level filtering, Dashboard Analytics, and Legacy Data Import capabilities.
- Client KYC: Support occupational/nickname suffix fields.
- CRIF High Mark Integration for credit bureau checks prior to disbursements (UAT and Production).

**User's preferred language**: English (INSTRUCT next agent to respond in English only)

**what currently exists?** 
A production-ready React + FastAPI + MongoDB MFI application with double-entry accounting, WebAuthn passkey login, and Vasuli Sheets. The CRIF High Mark Integration is now fully polished. It handles both UAT (IOI/CNS) and Production XML structures, rendering the loans in an intuitive per-loan accordion card format with a colored DPD payment history matrix. The app includes a Download PDF function for CRIF reports and strictly enforces a 30-day cooldown between pulls for a single client.

**Last working item**: 
    - Last item agent was working: Enforcing a 30-day cooldown on pulling new CRIF reports for a client to save API costs. The backend was updated to return a 429 with cooldown info, and the frontend Re-check button was updated to display a locked state (Locked · 30d) and an informative banner.
    - Status: COMPLETED
    - Agent Testing Done: Y
    - Which testing method agent to use? manual testing by curl/bash and screenshot tool.
    - User Testing Done: N

**All Pending/In progress Issue list**:  
None.

**In progress Task List**: 
None.

**Upcoming and Future Tasks**
    Upcoming Tasks:
    - Task 1 (P2): **WhatsApp Todays Collection Summary" share** on the Vasuli page (native device share using wa.me).
    - Task 2 (P2): **Print Passbook** (PDF/WhatsApp share) on the Client Detail tab.
    - Task 3 (P3): **Forgot Password OTP Flow** via SMS (Requires Twilio/Paid Gateway. Remember to use `integration_playbook_expert_v2` for the SMS/Auth integration).
    
    Future Tasks:
    - Task 4 (Enhancement): **Gyal Summary Dashboard Card** (Total bad debt amount/count overview).
    - Task 5 (Enhancement): **Manage Passkeys UI** section under the user profile page so field agents can view and delete registered devices without needing admin access.

**Completed work in this session**
- Fixed PROD CRIF XML parser truncation issue by increasing the string limit to 500,000 characters (`/app/backend/routes/crif.py`).
- Implemented accordion-style `LoanAccountCard` component for rendering both PROD and UAT/IOI loans with DPD payment history matrices (`/app/frontend/src/components/CrifCheck.jsx`).
- Changed CRIF Inquiry purpose code to "JLG Group" instead of "Housing Loan" (`/app/backend/routes/crif.py`).
- Added "Download PDF" button for the CRIF tab using custom print-media CSS (`/app/frontend/src/components/CrifCheck.jsx`, `/app/frontend/src/index.css`).
- Implemented a 30-day cooldown logic for fetching new CRIF reports (`/app/backend/routes/crif.py`, `/app/frontend/src/components/CrifCheck.jsx`).

**Earlier issues found/mentioned but not fixed**
   Issue 1: IP Whitelisting Stability
   - Debug checklist: The outbound IP currently used (`34.170.12.145`) is not guaranteed to remain stable across forks/deployments. The user was warned about this.
   - Why to solve this issue and what will be achieved with this? If the IP changes, CRIF API requests will fail due to their IP whitelisting rules. The agent suggested implementing a proxy service or contacting support.
   - Should Test frontend/backend/both after fix: Backend.
   - Is recurring issue? N

**Known issue recurrence from previous fork**
  - Issue recurrence in previous fork: None
  - Recurrence count: 0
  - Status: RESOLVED

**Code Architecture**
```text
/app/
├── backend/
│   ├── core/
│   ├── routes/ (collections.py, loans.py, kycs.py, crif.py)
│   ├── models.py, helpers.py, server.py
├── frontend/
│   └── src/
│       ├── components/ (CrifCheck.jsx, ClientDetail.jsx, ClientList.jsx)
```

**Key Technical Concepts**
React, Tailwind CSS, FastAPI, MongoDB, JWT Auth, CRIF High Mark XML SOAP Integration.

**key DB schema**
  - `kycs`: Stores customer details.
  - `crif_checks`: Stores `checked_at`, `raw_xml_response`, and `kyc_id`.

**changes in tech stack**
  - None.

**All files of reference** 
  - `/app/backend/routes/crif.py` (Updated XML limits, Inquiry Purpose, and Cooldown Logic).
  - `/app/frontend/src/components/CrifCheck.jsx` (Added `LoanAccountCard`, Payment matrix, PDF Download, and Cooldown lock states).
  - `/app/frontend/src/index.css` (Added `@media print` styling for the CRIF tab).
  - `/app/memory/PRD.md` (Updated).

**Areas that need refactoring**: 
- `CrifCheck.jsx` is growing very large (>800 lines). The `LoanAccountCard` component and `parsePaymentHistory` helper could be extracted into their own files to improve maintainability.

**key api endpoints**
- `POST /api/crif/check/{kyc_id}` (Initiate a new CRIF check, now blocks if within 30 days)
- `GET /api/crif/result/{kyc_id}` (Fetch latest stored CRIF result and cooldown state)
- `GET /api/crif/report-html/{kyc_id}` (Fallback for raw HTML reports)

**Critical Info for New Agent**
- **CRIF Cooldown**: There is now a strict 30-day cooldown logic on the `POST /api/crif/check` endpoint. Testing multiple checks for the same client will result in a 429 error.
- **Login Credentials**: The admin login expects a Mobile Number (`9999999999`) and Password (`Admin@123`).
- **IP Proxy Potential**: Be prepared to wire up a `CRIF_PROXY_URL` in the future if the user decides to solve the IP whitelisting instability with a static-IP proxy service.

**documents and test reports created in this job** 
  - `/app/memory/PRD.md` (Updated)
  - `/app/memory/test_credentials.md`

**Last 10 User Messages and any pending HUMAN messages** 
 - *User*: "Do not allow CRIF report to be pulled again for a particular client before 30 days have passed." (COMPLETED).

**Project Health Check:**
Healthy.

**3rd Party Integrations** 
  - Emergent Object Storage — requires Emergent Storage setup
  - Google Gemini (Aadhaar OCR) — uses Emergent LLM Key
  - WebAuthn (Passkeys) — browser standards/py_webauthn
  - CRIF High Mark (Credit Bureau) — requires User Credentials

**Testing status**
  - Testing agent used after significant changes: NO (Iterative manual testing used due to external API costs)
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created: None
  - Known regressions: None

**Credentials to test flow:**
Available in `/app/memory/test_credentials.md` (Admin: Mobile `9999999999` / `Admin@123`)

**What agent forgot to execute**
Everything requested was thoroughly executed and tested.

**Debugging checklist for current issue**
No active issue. For upcoming Task 1 (WhatsApp "Todays Collection Summary share):
    Frontend files to check: 
    -  (or equivalent Collection page where the summary is rendered).
  
    Backend files to check:
    - None required if purely extracting data from existing frontend state and generating a  link.</analysis>
